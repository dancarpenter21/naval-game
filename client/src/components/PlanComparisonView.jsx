import React from 'react';

const PlanComparisonView = () => {
  return (
    <div
      style={{
        padding: '2rem',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
        flexDirection: 'column',
      }}
    >
      <p style={{ fontSize: '1.5rem', color: '#aaa', textAlign: 'center' }}>Plan Comparison</p>
    </div>
  );
};

export default PlanComparisonView;
