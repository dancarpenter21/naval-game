# SIDC picker (removed Docker build)

The interactive SIDC builder now ships with the **client** as static files (no separate image):

- `client/public/sidc-picker/index.html`

Use **Hamburger → SIDC builder** or open `/sidc-picker/index.html` via nginx/Vite.
