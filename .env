# This env file is to help set up the Docker Compose network hosts.

# For Linux/macOS
HOSTNAME=$(hostname)

# For Windows PowerShell (uncomment this line and comment the one above)
# HOSTNAME=$($env:computername)
